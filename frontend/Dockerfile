FROM node:20-alpine

WORKDIR /app

# Copy package files
COPY package.json package-lock.json* ./

# Install dependencies
RUN npm install

# Copy source code
COPY . .

EXPOSE 3000

# Development mode - reinstall deps on start to handle volume mounts
CMD ["sh", "-c", "npm install && npm run dev"]
